# test/

Test infrastructure:

- **setup.ts** — Vitest global setup (e.g., jest-dom matchers).
- **test-utils.tsx** — Custom render wrapper with all providers.
- **mocks/** — MSW handlers and mock data.
